<nav>
    <h1 class="logo">
        <a href="{{ path('app_register') }}" title="logo sortir.com">sortir.com</a>
    </h1>
    <ul>
        {% if is_granted("ROLE_ADMIN") %}
            <li>
                <a href="{{ path('app_register' )}}" title="villes">Villes</a>
                <a href="{{ path('app_register')}}" title="Campus">Campus</a>
            </li>
        {% endif %}
        <li>
            <a href="{{ path('app_register' )}}" title="Accueil">Accueil</a>
            <a href="{{ path('app_register')}}" title="profil">Mon profil</a>
            <a href="{{ path('app_register' )}}" title="déconnexion">Déconnexion</a>
        </li>
    </ul>
</nav>