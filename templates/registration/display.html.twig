{% extends 'base.html.twig' %}

{% block title %}Register{% endblock %}

{% block body %}
    <h1>Profil de {{ app.user }}</h1>

    {{ form_start(registrationForm) }}

    {{ form_widget((registrationForm))}}

    {{ form_end(registrationForm) }}
{% endblock %}