{% extends 'base.html.twig' %}

{% block title %}{{ parent() }} | Afficher une sortie{% endblock %}

{% block body %}
    <div>
        <div>
            <h2>Nom de la sortie : {{ sortie.nom }}</h2>
    
            <div>Date et heure de la sorti : {{ sortie.dateHeureDebut }}</div>
            <div>Date limite d'inscription : {{ sortie.dateLimiteInscription }}</div>
            <div>Nombre de places : {{ sortie.nbInscriptionsMax }}</div>
            <div>Durée : {{ sortie.duree }} minutes</div>
            <div>Description et infos : {{ sortie.infosSortie }}</div>
        </div>
        <div>
            <div>Campus : {{ sortie.siteOrganisateur }}</div>
            <div>Lieu : {{ sortie.lieu }}</div>
            <div>Rue : {{ sortie.rue }}</div>
            <div>Code Postal : {{ sortie.codePostal }}</div>
            <div>Latitude : {{ sortie.latitude }}</div>
            <div>Longitude : {{ sortie.longitude }}</div>
        </div>
    </div>
    
    <div>
        <p>Liste des participants inscrits:</p>
        {% for inscrit in participant.estInscrit %}
            TODO:faire un tableau
        {% endfor %}
    </div>
{% endblock %}